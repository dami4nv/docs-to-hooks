# UI interaction guide

Every action needs a keyboard path. Keep focus visible when moving between
catalog entries. An empty catalog should explain what happened and offer a
useful next step. Loading, empty, and error states are distinct; do not collapse
them. Check these states in the UI, not just in a screenshot of populated data.
